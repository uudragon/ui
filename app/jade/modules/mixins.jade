mixin tooltip(title)
	a(href='', uu-tooltip, data-placement='bottom', data-toggle='tooltip', title=title)
		if block
			block

mixin glyphicon(className)
	span.glyphicon(class='glyphicon-' + className)
		if block
			block