
//- Inputs
//- ----------------------

//- 3 label, 7 input
mixin input(title, id)
	label.control-label.col-xs-3(for='form-' + id)=title
	.col-xs-7
		if !attributes['uu-date-picker']
			input.form-control.input-sm(id='form-' + id, ng-click=attributes['ng-click'], ng-model=attributes['ng-model'])
		else
			input.form-control.input-sm(id='form-' + id, ng-click=attributes['ng-click'], ng-model=attributes['ng-model'], uu-date-picker='', min-view=attributes['min-view'], format=attributes['format'])

mixin static-input(title)
	label.control-label.col-xs-3=title
	.col-xs-7
		p.form-control-static(ng-bind=attributes['ng-bind'])

//- 4 label, 6 input
mixin input-tweak(title, id)
	label.control-label.col-xs-4(for='form-' + id)=title
	.col-xs-6
		if !attributes['uu-date-picker']
			input.form-control.input-sm(id='form-' + id, ng-click=attributes['ng-click'], ng-model=attributes['ng-model'])
		else
			input.form-control.input-sm(id='form-' + id, ng-click=attributes['ng-click'], ng-model=attributes['ng-model'], uu-date-picker='', min-view=attributes['min-view'], format=attributes['format'])


//- free label, free input
mixin plain-input(title, id)
	label.control-label(for='form-' + id)=title
	if !attributes['uu-date-picker']
		input.form-control.input-sm(id='form-' + id, ng-click=attributes['ng-click'], ng-model=attributes['ng-model'])
	else
		input.form-control.input-sm(id='form-' + id, ng-click=attributes['ng-click'], ng-model=attributes['ng-model'], uu-date-picker='', min-view=attributes['min-view'], format=attributes['format'])

//- 3 label, 7 input, message-multi
mixin short-input(title, id)
	label.control-label.col-xs-3(for='form-' + id)=title
	.col-xs-7.message-multi
		if !attributes['uu-date-picker']
			input.form-control.input-sm(id='form-' + id, ng-click=attributes['ng-click'], ng-model=attributes['ng-model'])
		else
			input.form-control.input-sm(id='form-' + id, ng-click=attributes['ng-click'], ng-model=attributes['ng-model'], uu-date-picker='', min-view=attributes['min-view'], format=attributes['format'])
		if block
			block

//- input addon
mixin grouped-input(title, id)
	.input-group(ng-show=attributes['ng-show'])
		label.input-group-addon(for='form-' + id)=title
		if !attributes['uu-date-picker']
			input.form-control.input-sm(id='form-' + id, ng-click=attributes['ng-click'], ng-model=attributes['ng-model'])
		else
			input.form-control.input-sm(id='form-' + id, ng-click=attributes['ng-click'], ng-model=attributes['ng-model'], uu-date-picker='', min-view=attributes['min-view'], format=attributes['format'])


//- Textarea
//- ----------------------

//- 3 label, 7 from-control
mixin textarea(title, id)
	label.control-label.col-xs-3(for='form-' + id)=title
	.col-xs-7
		textarea.form-control(id='form-' + id, ng-click=attributes['ng-click'], ng-model=attributes['ng-model'])

mixin plain-textarea(title, id)
	label.control-label(for='form-' + id)=title
	textarea.form-control(id='form-' + id, ng-click=attributes['ng-click'], ng-model=attributes['ng-model'])



//- Select
//- ----------------------

//- 3 label, 7 from-control
mixin select(title, id)
	label.control-label.col-xs-3(for='form-' + id)=title
	.col-xs-7
		select.form-control.input-sm(id='form-' + id, name=attributes['name'], ng-model=attributes['ng-model'], ng-options=attributes[ng-options])
			block

//- 3 label, 7 from-control, multi-row
mixin multi-select(title, id)
	label.control-label.col-xs-3(for='form-' + id)=title
	.col-xs-7
		select.form-control(id='form-' + id, name=attributes['name'], ng-model=attributes['ng-model'], size='5', ng-options=attributes['ng-options'])
			block

//- 4 label, 6 from-control
mixin select-tweak(title, id)
	label.control-label.col-xs-4(for='form-' + id)=title
	.col-xs-6
		select.form-control.input-sm(id='form-' + id, name=attributes['name'], ng-model=attributes['ng-model'])
			block

//- free label, free from-control
mixin plain-select(title, id, options)
	if title
		label.control-label(for='form-' + id)=title
	select.form-control.input-sm(id='form-' + id, name=attributes['name'], ng-model=attributes['ng-model'])
		if options
			each val in options
				+option(val.title, val.value)

mixin option(title, value)
	if value === null
		- var value = title
	option(value=value, ng-show=attributes['ng-show'], ng-if=attributes['ng-if'])=title


//- bootstrap short input helper
mixin submit(title, id)
	.col-xs-7.col-xs-offset-3
		if block
			block


mixin gender(title, id)
	+select(title, id)(ng-model=attributes['ng-model'])
		+option('女', 0)
		+option('男', 1)

mixin order-status(title, id)
	+select(title, id)(ng-model=attributes['ng-model'])
		+option('待审核', 1)
		+option('审核中', 2)
		+option('无效', 3)
		+option('审核通过', 4)

mixin workflow-group(title, id)
	+select(title, id)(ng-model=attributes['ng-model'])
		+option('审单组', 1)
		+option('拆单组', 2)

mixin complain-type(title, id)
	+select(title, id)(ng-model=attributes['ng-model'])
		+option('发票抬头错误', 31)
		+option('未开发票', 32)
		+option('开票时间长', 33)
		+option('发票丢失', 34)
		+option('客服态度不好', 35)
		+option('客服不专业', 36)
		+option('客服电话难打', 37)
		+option('物流慢', 38)
		+option('货物丢失', 39)
		+option('物品破损', 310)
		+option('快递态度', 311)

mixin urgency-level(title, id)
	+select('紧急程度', 'urgency')(ng-model='contact.level')
		+option('一般', 1)
		+option('优先', 2)
		+option('紧急', 3)

mixin deal-result(title, id)
	+select('处理结果', 'deal-result')(ng-model='contact.status')
		+option('待处理', 1)
		+option('处理中', 2)
		+option('已完成', 3)
